<Window x:Class="SimpleImageViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Image Viewer" 
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Black" 
        ResizeMode="CanResize"
        MouseDown="Window_MouseDown" 
        MouseRightButtonUp="Window_MouseRightButtonUp"
        KeyDown="Window_KeyDown"
        SourceInitialized="Window_SourceInitialized">
    <Grid>
        <!-- Image Display -->
        <Image x:Name="ImageDisplay" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" />

        <!-- Context Menu -->
        <Grid.ContextMenu>
            <ContextMenu x:Name="ImageContextMenu">
                <MenuItem Header="Open Image..." Click="OpenImage_Click"/>
                <MenuItem Header="Exit" Click="Exit_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</Window>
